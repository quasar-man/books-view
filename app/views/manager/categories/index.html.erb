<div class="content">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>CreatedAt</th>
        <th>UpdatedAt</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @categories.each do |category| %>
        <tr>
          <td><%= category.id %></td>
          <td><%= category.name %></td>
          <td><%= category.created_at %></td>
          <td><%= category.updated_at %></td>
          <td>
            <%= link_to "Edit", edit_manager_category_path(category), class: "btn btn-success me-md-2" %>
            <%= link_to "Delete", manager_category_path(category), :method => :delete, :class => "btn btn-danger me-md-2" %>
          </td>
        </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="5" class="add-btn-area">
          <%= link_to "Add", new_manager_category_path, :class => "btn btn-primary me-md-2" %>
        </td>
      </tr>
    </tfoot>
  </table>
</div>