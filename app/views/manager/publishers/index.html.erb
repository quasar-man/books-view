<div class="content">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>CreatedAt</th>
        <th>UpdatedAt</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @publishers.each do |publisher| %>
        <tr>
          <td><%= publisher.id %></td>
          <td><%= publisher.name %></td>
          <td><%= publisher.created_at %></td>
          <td><%= publisher.updated_at %></td>
          <td>
            <%= link_to "Edit", edit_manager_publisher_path(publisher), class: "btn btn-success me-md-2" %>
            <%= link_to "Delete", manager_publisher_path(publisher), :method => :delete, :class => "btn btn-danger me-md-2" %>
          </td>
        </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="5" class="add-btn-area">
          <%= link_to "Add", new_manager_publisher_path, :class => "btn btn-primary me-md-2" %>
        </td>
      </tr>
    </tfoot>
  </table>
</div>